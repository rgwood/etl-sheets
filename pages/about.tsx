import '../styles/index.css'
import Layout from '../components/Layout'

export default () => (
  <Layout title="About Page">
    <p>An experiment in user interfaces for ETL (Extract-Transform-Load) processes (ok, mostly just the Transform part). 
      Users are very good at transforming data in spreadsheets. Can we give them a spreadsheet-like interface for more automated transforms?
    </p>
  </Layout>
)